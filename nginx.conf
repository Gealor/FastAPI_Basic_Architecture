user root;
worker_processes 1;

events{
}

http{
    server {
        listen          80;
        server_name     192.168.1.54;
        # server_name    localhost;
        location / {
            proxy_pass  http://backend:8000/;
        }
    }
}